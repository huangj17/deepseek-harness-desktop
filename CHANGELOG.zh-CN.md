# 更新日志

[English](CHANGELOG.md) · **简体中文**

本文件记录桌面客户端每个版本的变化。发布流水线会从这里和 [CHANGELOG.md](CHANGELOG.md) 取出对应版本的小节作为 GitHub Release 的说明，所以发版前两份都要补上一节。

## 0.2.11

- 新增：侧栏底部加入“打开 DSH 终端”，可在当前会话或最近工作区直接打开系统终端。终端内的 `dsh` 与桌面客户端使用同一个 Harness 数据目录和当前生效的内置或下载版运行时，无需全局安装。
- 优化：关闭主窗口后，Harness 与进行中的任务会继续在后台运行；点击应用图标可恢复窗口，也可从 Dock、任务栏或系统托盘菜单完全退出客户端。
- 可靠性与安全性：仅接受来自 Harness 主页面的终端请求，并校验绝对工作目录；Electron Node 模式只作用于 `dsh` 包装器，独立启动脚本可避免并发打开终端时进入错误工作区。
- 兼容：如果桌面终端插件导致未来的 Harness 运行时启动失败，客户端会自动去掉该入口重试，确保 Harness 主界面仍可使用。

## 0.2.10

- 修复：应用内将 Harness 更新到 `0.1.1-rc.2` 或更高版本后，启动桌面客户端还会在系统默认浏览器中重复打开 Harness 页面。客户端现在会为支持该参数的运行时传入 `--no-open`，Web UI 只保留在 Electron 窗口中。
- 兼容：旧版内置 Harness 不认识 `--no-open`，启动器会继续使用原参数，仅对已经支持该选项的版本启用，避免旧运行时启动失败。
- 测试：源码与打包后运行时冒烟测试统一复用按版本生成的启动参数，防止以后升级内置 Harness 时再次引入浏览器跳转。

## 0.2.9

- 新增：应用内检查客户端自身的更新。启动后及每 6 小时查询 GitHub 发布页，发现新版会弹窗提示，可直接前往下载对应平台的安装包。
- 新增：更新提示支持「跳过此版本」，跳过后该版本不再打扰；从菜单主动点「检查客户端更新…」时仍会正常提示。
- 说明：客户端更新仍需手动安装新包（安装包未做 Apple Developer ID 签名，无法静默自动更新）。Harness 运行时的自动更新不受影响。

## 0.2.8

- 修复：Windows 上点「打开文件夹」选中目录后报 `win32 folder dialog worker exited before reporting a result`，无法打开工作区。根因在上游 Harness 的原生目录选择器会读越界导致子进程崩溃，客户端在 Windows 上改用网页内的目录浏览器规避。
- 修复：从 Finder / 开始菜单启动时，更新 Harness 会因为找不到 `node` 而失败（退出码 127）。现在安装过程自带 node 垫片，不再依赖系统 PATH。
- 优化：更新进度窗重新设计，去掉重复的卡片边框和 macOS 假标题栏；Windows / Linux 上不再显示多余的菜单栏和滚动条。

## 0.2.7

- 新增：提供 macOS（Apple 芯片 / Intel）、Windows、Linux 全平台安装包。
- 修复：Linux 软件包元数据不完整的问题。
- 优化：明确客户端与官方 Harness 的关系，避免与官方发布混淆。

## 0.2.6

- 首个开源发布版本。
